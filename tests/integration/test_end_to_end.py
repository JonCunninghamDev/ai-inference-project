"""Integration tests for Air-Gapped RAG system."""\n\nimport os\nimport sys\nimport json\nimport time\nimport pytest\nimport boto3\nfrom unittest.mock import patch, MagicMock\nfrom moto import mock_sqs, mock_ssm, mock_logs\n\n# Add src directory to path\nsys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..', 'src'))\n\nfrom ai_inference.core.config import ConfigManager, WorkerConfig\nfrom ai_inference.core.worker import RAGWorker, RAGTask\n\n\nclass TestConfigManager:\n    \"\"\"Test configuration management.\"\"\"\n    \n    def test_load_dev_config(self):\n        \"\"\"Test loading development configuration.\"\"\"\n        config_manager = ConfigManager(\"dev\")\n        config = config_manager.get_config()\n        \n        assert isinstance(config, WorkerConfig)\n        assert config.aws_region == \"us-east-1\"\n        assert config.queue_name == \"ai-inference-queue-Dev\"\n        assert config.vllm_url == \"http://localhost:8000/v1\"\n    \n    def test_config_validation(self):\n        \"\"\"Test configuration validation.\"\"\"\n        # Test invalid vLLM URL\n        with patch.dict(os.environ, {\"VLLM_URL\": \"invalid-url\"}):\n            with pytest.raises(ValueError, match=\"vLLM URL must start with\"):\n                ConfigManager(\"dev\")\n    \n    def test_queue_url_generation(self):\n        \"\"\"Test SQS queue URL generation.\"\"\"\n        config = ConfigManager(\"dev\").get_config()\n        expected_url = f\"https://sqs.{config.aws_region}.amazonaws.com/{config.aws_account_id}/{config.queue_name}\"\n        assert config.queue_url == expected_url\n\n\n@mock_sqs\n@mock_ssm\n@mock_logs\nclass TestRAGWorker:\n    \"\"\"Test RAG worker functionality.\"\"\"\n    \n    def setup_method(self):\n        \"\"\"Setup test environment.\"\"\"\n        # Create mock AWS resources\n        self.sqs = boto3.client(\"sqs\", region_name=\"us-east-1\")\n        self.ssm = boto3.client(\"ssm\", region_name=\"us-east-1\")\n        self.logs = boto3.client(\"logs\", region_name=\"us-east-1\")\n        \n        # Create SQS queue\n        self.queue_response = self.sqs.create_queue(\n            QueueName=\"ai-inference-queue-Dev\"\n        )\n        self.queue_url = self.queue_response[\"QueueUrl\"]\n        \n        # Create SSM parameter\n        self.ssm.put_parameter(\n            Name=\"/ai-inference/config/Dev/enable-processing\",\n            Value=\"true\",\n            Type=\"String\"\n        )\n        \n        # Create CloudWatch log group\n        self.logs.create_log_group(\n            logGroupName=\"/ai-inference/worker-Dev\"\n        )\n    \n    def test_worker_initialization(self):\n        \"\"\"Test worker initialization.\"\"\"\n        with patch('worker.OpenAI') as mock_openai:\n            mock_client = MagicMock()\n            mock_openai.return_value = mock_client\n            \n            worker = RAGWorker(\"dev\")\n            \n            assert worker.config.environment == \"dev\"\n            assert worker.health is not None\n            assert worker.ai_client == mock_client\n    \n    def test_kill_switch_check(self):\n        \"\"\"Test kill switch functionality.\"\"\"\n        with patch('worker.OpenAI'):\n            worker = RAGWorker(\"dev\")\n            \n            # Test enabled state\n            assert worker.check_kill_switch() is True\n            \n            # Test disabled state\n            self.ssm.put_parameter(\n                Name=\"/ai-inference/config/Dev/enable-processing\",\n                Value=\"false\",\n                Type=\"String\",\n                Overwrite=True\n            )\n            assert worker.check_kill_switch() is False\n    \n    def test_message_processing(self):\n        \"\"\"Test SQS message processing.\"\"\"\n        with patch('worker.OpenAI') as mock_openai:\n            # Setup mock AI client\n            mock_client = MagicMock()\n            mock_response = MagicMock()\n            mock_response.choices = [MagicMock()]\n            mock_response.choices[0].message.content = \"Test response\"\n            mock_client.chat.completions.create.return_value = mock_response\n            mock_openai.return_value = mock_client\n            \n            worker = RAGWorker(\"dev\")\n            \n            # Send test message\n            test_task = {\n                \"prompt\": \"Test prompt\",\n                \"context\": \"Test context\",\n                \"event_type\": \"test\"\n            }\n            \n            self.sqs.send_message(\n                QueueUrl=self.queue_url,\n                MessageBody=json.dumps(test_task)\n            )\n            \n            # Receive and process message\n            response = self.sqs.receive_message(\n                QueueUrl=self.queue_url,\n                MaxNumberOfMessages=1\n            )\n            \n            messages = response.get(\"Messages\", [])\n            assert len(messages) == 1\n            \n            # Process the message\n            result = worker.process_message(messages[0])\n            assert result is True\n            \n            # Verify AI client was called\n            mock_client.chat.completions.create.assert_called_once()\n    \n    def test_health_monitoring(self):\n        \"\"\"Test health monitoring functionality.\"\"\"\n        with patch('worker.OpenAI'):\n            worker = RAGWorker(\"dev\")\n            \n            # Initial health should be good\n            assert worker.health.is_healthy is True\n            assert worker.health.messages_processed == 0\n            assert worker.health.errors_count == 0\n            \n            # Record success\n            worker.health.record_success()\n            assert worker.health.messages_processed == 1\n            \n            # Record error\n            worker.health.record_error(\"Test error\")\n            assert worker.health.errors_count == 1\n            assert worker.health.last_error == \"Test error\"\n\n\nclass TestRAGTask:\n    \"\"\"Test RAG task model.\"\"\"\n    \n    def test_valid_task_creation(self):\n        \"\"\"Test creating valid RAG task.\"\"\"\n        task_data = {\n            \"prompt\": \"Test prompt\",\n            \"context\": \"Test context\",\n            \"event_type\": \"test\"\n        }\n        \n        task = RAGTask(**task_data)\n        assert task.prompt == \"Test prompt\"\n        assert task.context == \"Test context\"\n        assert task.event_type == \"test\"\n    \n    def test_task_defaults(self):\n        \"\"\"Test RAG task default values.\"\"\"\n        task_data = {\n            \"prompt\": \"Test prompt\",\n            \"context\": \"Test context\"\n        }\n        \n        task = RAGTask(**task_data)\n        assert task.event_type == \"general_inference\"\n        assert task.event_id is None\n        assert task.timestamp is None\n    \n    def test_invalid_task_creation(self):\n        \"\"\"Test creating invalid RAG task.\"\"\"\n        # Missing required fields\n        with pytest.raises(ValueError):\n            RAGTask(prompt=\"Test prompt\")  # Missing context\n        \n        with pytest.raises(ValueError):\n            RAGTask(context=\"Test context\")  # Missing prompt\n\n\n@mock_sqs\n@mock_ssm\nclass TestEndToEndFlow:\n    \"\"\"Test end-to-end system flow.\"\"\"\n    \n    def setup_method(self):\n        \"\"\"Setup test environment.\"\"\"\n        # Create mock AWS resources\n        self.sqs = boto3.client(\"sqs\", region_name=\"us-east-1\")\n        self.ssm = boto3.client(\"ssm\", region_name=\"us-east-1\")\n        \n        # Create SQS queue\n        self.queue_response = self.sqs.create_queue(\n            QueueName=\"ai-inference-queue-Dev\"\n        )\n        self.queue_url = self.queue_response[\"QueueUrl\"]\n        \n        # Create DLQ\n        self.dlq_response = self.sqs.create_queue(\n            QueueName=\"ai-inference-dlq-Dev\"\n        )\n        \n        # Create SSM parameter\n        self.ssm.put_parameter(\n            Name=\"/ai-inference/config/Dev/enable-processing\",\n            Value=\"true\",\n            Type=\"String\"\n        )\n    \n    def test_complete_message_flow(self):\n        \"\"\"Test complete message processing flow.\"\"\"\n        with patch('worker.OpenAI') as mock_openai:\n            # Setup mock AI client\n            mock_client = MagicMock()\n            mock_response = MagicMock()\n            mock_response.choices = [MagicMock()]\n            mock_response.choices[0].message.content = \"AI generated response\"\n            mock_client.chat.completions.create.return_value = mock_response\n            mock_client.models.list.return_value = MagicMock()  # For initialization\n            mock_openai.return_value = mock_client\n            \n            # Create worker\n            worker = RAGWorker(\"dev\")\n            \n            # Send message to queue\n            test_message = {\n                \"prompt\": \"What is the capital of France?\",\n                \"context\": \"France is a country in Europe.\",\n                \"event_type\": \"geography_question\",\n                \"event_id\": \"test-123\"\n            }\n            \n            self.sqs.send_message(\n                QueueUrl=self.queue_url,\n                MessageBody=json.dumps(test_message)\n            )\n            \n            # Verify message is in queue\n            attrs = self.sqs.get_queue_attributes(\n                QueueUrl=self.queue_url,\n                AttributeNames=['ApproximateNumberOfMessages']\n            )\n            assert int(attrs['Attributes']['ApproximateNumberOfMessages']) == 1\n            \n            # Process message\n            response = self.sqs.receive_message(\n                QueueUrl=self.queue_url,\n                MaxNumberOfMessages=1\n            )\n            \n            messages = response.get(\"Messages\", [])\n            assert len(messages) == 1\n            \n            # Process the message\n            result = worker.process_message(messages[0])\n            assert result is True\n            \n            # Verify message was deleted from queue\n            attrs = self.sqs.get_queue_attributes(\n                QueueUrl=self.queue_url,\n                AttributeNames=['ApproximateNumberOfMessages']\n            )\n            assert int(attrs['Attributes']['ApproximateNumberOfMessages']) == 0\n            \n            # Verify AI was called with correct parameters\n            call_args = mock_client.chat.completions.create.call_args\n            assert \"What is the capital of France?\" in str(call_args)\n            assert \"France is a country in Europe.\" in str(call_args)\n    \n    def test_kill_switch_prevents_processing(self):\n        \"\"\"Test that kill switch prevents message processing.\"\"\"\n        with patch('worker.OpenAI') as mock_openai:\n            mock_client = MagicMock()\n            mock_openai.return_value = mock_client\n            \n            # Disable processing\n            self.ssm.put_parameter(\n                Name=\"/ai-inference/config/Dev/enable-processing\",\n                Value=\"false\",\n                Type=\"String\",\n                Overwrite=True\n            )\n            \n            worker = RAGWorker(\"dev\")\n            \n            # Send message\n            test_message = {\n                \"prompt\": \"Test prompt\",\n                \"context\": \"Test context\"\n            }\n            \n            self.sqs.send_message(\n                QueueUrl=self.queue_url,\n                MessageBody=json.dumps(test_message)\n            )\n            \n            # Check kill switch\n            assert worker.check_kill_switch() is False\n            \n            # Verify AI client is not called when kill switch is off\n            # (In real scenario, worker would skip processing)\n            mock_client.chat.completions.create.assert_not_called()\n\n\nif __name__ == \"__main__\":\n    pytest.main([__file__, \"-v\"])